## 取任意区间值

```js
Math.random() * (max - min) + min;
```

取任意区间整数值：

```javascript
Math.round(Math.random() * (max - min)) + min;
```





# 取整

```js
rounded = (0.5 + somenum) | 0;
rounded = ~~ (0.5 + somenum);
rounded = (0.5 + somenum) << 0;
```

