## throtle
```javascript
function throtle (fn, wait){
    var timer;
    return function(...args){
        if (!timer) {
            timer = setTimeout(() => {
                timer = null;
            }, wait);
            return fn.apply(this, agrs)
        }
    }
}
```


## compose
```javascript
    var compose = function(f, g){
        return function(x) {
            return f(g(x));
        }
    }
```

